const {app,net}=require('electron'),fs=require('fs'),path=require('path'),os=require('os');app.setPath('userData',fs.mkdtempSync(path.join(os.tmpdir(),'yueden-240-probe-')));app.whenReady().then(async()=>{const results=[];for(const [id,url]of [['book','https://book.douban.com/subject/2567698/'],['movie','https://movie.douban.com/subject/1292052/'],['tstrs','https://tstrs.me/search'],['tstrs-book','https://tstrs.me/book/1bdiq630sml'],['1lib','https://zh.1lib.sk/']]){try{const r=await net.fetch(url,{signal:AbortSignal.timeout(12000)}),body=await r.text();fs.writeFileSync(path.resolve('test-artifacts/release-2.25.40/'+id+'.html'),body);results.push({id,status:r.status,url:r.url,bytes:body.length});}catch(e){results.push({id,error:e.message});}}fs.writeFileSync('test-artifacts/release-2.25.40/probe.json',JSON.stringify(results,null,2));app.exit();});