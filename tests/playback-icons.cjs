const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm');test('shared transport SVGs keep labels and state with no glyph fallback',()=>{const ctx={window:{}};vm.runInNewContext(fs.readFileSync(require.resolve('../app/playback-controls'),'utf8'),ctx);const b={dataset:{},classList:{add(){},toggle(){}},setAttribute(k,v){this[k]=v}};for(const name of ['previous','next','play','pause','stop','volume','muted','repeat','single','shuffle','queue','lyrics','eq','fullscreen','exitFullscreen']){ctx.window.PlaybackControls.set(b,name,'label');assert.match(b.innerHTML,/<svg/);assert.doesNotMatch(b.innerHTML,/undefined/);assert.equal(b['aria-label'],'label');assert.equal(b.title,'label');assert.equal(b.dataset.playbackIcon,name);}});