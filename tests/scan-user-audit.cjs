// Read-only real-directory audit. Private paths stay in local test-artifacts, not the package.
const fs=require('node:fs'),path=require('node:path');const app=path.resolve(__dirname,'../app');
(async()=>{const roots=process.argv.slice(2);if(!roots.length)throw Error('必须提供扫描根目录');const r=await require(path.join(app,'scan-pipeline')).scan(roots,'game',{online:false});const report={at:new Date().toISOString(),roots,metrics:r.metrics,warnings:r.warnings,count:r.items.length,items:r.items.map(i=>({name:i.name,type:i.classification.type,path:i.localPath,files:i.localFiles.map(f=>f.path)})),software:r.candidates.filter(i=>i.type==='software').map(i=>({name:i.name,path:i.localPath,reason:i.classification.reason})),skipped:r.skipped.filter(i=>i.reasonCode==='permission-or-missing')};fs.writeFileSync(path.resolve(__dirname,'../test-artifacts/user-scan-audit.json'),JSON.stringify(report,null,2));console.log(JSON.stringify({count:report.count,software:report.software.length,metrics:report.metrics,warnings:report.warnings,reviewed:report.items.filter(i=>/AKPlatform|citra|DangoTranslator|Dalamud|HunterPie|Stardrop|SMAPI|ReShade|MHWShopEditor|Meal-Editor|Astlibra|狩技|春へ/.test(i.path))},null,2));})().catch(e=>{console.error(e);process.exitCode=1;});
