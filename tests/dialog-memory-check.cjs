const fs=require('node:fs'),os=require('node:os'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),'um-dialog-')),a=path.join(root,'a'),b=path.join(root,'b');fs.mkdirSync(a);fs.mkdirSync(b);const file=path.join(a,'note.md');fs.writeFileSync(file,'test');let options;const dialog=require('../app/dialog-memory').wrap({showOpenDialog:async v=>{options=v;return {canceled:false,filePaths:[file]};}},()=>root);await dialog.showOpenDialog({title:'files'});await dialog.showOpenDialog({title:'files'});assert.equal(options.defaultPath,a);await dialog.showOpenDialog({title:'files',defaultPath:b});assert.equal(options.defaultPath,b);await dialog.showOpenDialog({title:'files',fallbackPath:b});assert.equal(options.defaultPath,a);console.log('3 dialog history assertions passed');})().catch(e=>{console.error(e);process.exitCode=1;});
