const {spawn}=require('node:child_process'),path=require('node:path'),fs=require('node:fs');const root=path.resolve(__dirname,'..'),dest=path.join(root,'test-artifacts/task1-dialog');fs.mkdirSync(dest,{recursive:true});const env={...process.env,UM_SCANNER_CHECKS:path.join(__dirname,process.argv[2]||'native-import-dialog-stack.cjs'),UM_NATIVE_REPORT_PATH:path.join(dest,process.argv[2]?path.basename(process.argv[2],'.cjs')+'-result.json':'native-result.json'),UM_NATIVE_SYNTHETIC:'1'};delete env.ELECTRON_RUN_AS_NODE;const child=spawn(path.resolve(root,'../runtime-44/extracted/electron.exe'),[path.join(__dirname,'native-scanner-only.cjs')],{env,windowsHide:true,stdio:'inherit'});const timer=setTimeout(()=>{child.kill();console.error('Native test timed out');process.exitCode=1},100000);child.on('error',e=>{clearTimeout(timer);console.error(e);process.exitCode=1});child.on('exit',code=>{clearTimeout(timer);console.log('Native import dialog checks exit: '+code);process.exitCode=code===0?0:1});