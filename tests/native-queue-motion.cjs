module.exports=async({w,js,check})=>{await js(w,'new Promise((resolve,reject)=>{const script=document.createElement("script");script.src="player/queue-reorder.js";script.onload=resolve;script.onerror=reject;document.head.append(script)})');const result=await js(w,`(async()=>{const host=document.createElement('ul');document.body.append(host);for(const id of ['a','b','c']){const row=document.createElement('li');row.dataset.id=id;row.draggable=true;row.textContent=id;row.tabIndex=0;row.style.height='40px';host.append(row);}const commits=[];installQueueReorder(host,async ids=>commits.push(ids.join('')),e=>{throw e});const order=()=>[...host.children].map(n=>n.dataset.id).join('');const drag=(from,to,after)=>{const dataTransfer=new DataTransfer(),a=host.querySelector('[data-id='+from+']'),b=host.querySelector('[data-id='+to+']');a.dispatchEvent(new DragEvent('dragstart',{bubbles:true,dataTransfer}));const r=b.getBoundingClientRect();b.dispatchEvent(new DragEvent('dragover',{bubbles:true,cancelable:true,dataTransfer,clientY:after?r.bottom-1:r.top+1}));return {a,b,dataTransfer};};let d=drag('a','c',true);const preview=order(),beforeDrop=commits.length;d.b.dispatchEvent(new DragEvent('drop',{bubbles:true,cancelable:true,dataTransfer:d.dataTransfer}));await Promise.resolve();const first=order();d=drag('a','b',false);d.b.dispatchEvent(new DragEvent('drop',{bubbles:true,cancelable:true,dataTransfer:d.dataTransfer}));await Promise.resolve();const last=order();d=drag('a','c',true);d.a.dispatchEvent(new DragEvent('dragend',{bubbles:true,dataTransfer:d.dataTransfer}));const cancelled=order(),afterCancel=commits.length;host.firstChild.dispatchEvent(new KeyboardEvent('keydown',{key:'ArrowDown',altKey:true,bubbles:true,cancelable:true}));await Promise.resolve();const keyboard=order();host.remove();return {preview,beforeDrop,first,last,cancelled,afterCancel,keyboard,commits};})()`);check('first to last preview, no persistence while dragging',result.preview==='bca'&&result.beforeDrop===0);check('drop saves once and reverse drag works',result.first==='bca'&&result.last==='abc'&&result.commits[0]==='bca'&&result.commits[1]==='abc');check('cancel restores order without saving',result.cancelled==='abc'&&result.afterCancel===2);check('keyboard moves and saves once',result.keyboard==='bac'&&result.commits.length===3);};