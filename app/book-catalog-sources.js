const H=require('./metadata-html'),Runtime=require('./metadata-runtime');
const origins={tstrs:'https://tstrs.me',onelib:'https://zh.1lib.sk'};
function url(value,origin){try{const u=new URL(value,origin);return u.origin===origin&&/^\/book\/[\w-]+(?:\/[\w%-]+)?\/?$/.test(u.pathname)?u.href:'';}catch{return '';}}
function parseSearch(html,id){if(!html)return [];const origin=origins[id],blocks=id==='tstrs'?H.blocks(html,'div',a=>/^sr-\d+$/.test(a.id||'')):H.blocks(html,'a',a=>Boolean(url(a.href,origin)));const result=[];for(const block of blocks){const link=id==='tstrs'?H.attributes(block.body.match(/<a\b[^>]*>/i)?.[0]||'').href:block.attrs.href;const target=url(link,origin);if(!target||result.some(v=>v.storeUrl===target))continue;const title=id==='tstrs'?H.text(block.body.match(/<b[^>]*>([\s\S]*?)<\/b>/i)?.[1]):H.text(block.body);if(!title)continue;result.push({id:id+'-'+new URL(target).pathname.split('/')[2],name:title,storeUrl:target,detailsUnavailable:true});}if(!result.length&&!/没有找到|未找到相关|没有搜索到|No books found|Nothing found|0 results/i.test(H.text(html)))throw new SyntaxError('书籍搜索页结构无法识别');return result.slice(0,5);}
function field(value){const text=H.text(value);return /^(?:【)?(?:未找到(?:作者|出版社)?信息|暂无(?:简介|描述|信息)|无简介)(?:】)?$/.test(text)?'':text;}
function parseDetail(html,seed){if(!html)return seed;const data=H.structured(html,'Book')[0];if(!data?.name)throw new SyntaxError('书籍详情缺少可识别的公开 Book 元数据');const person=v=>[v].flat().filter(Boolean).map(v=>field(typeof v==='string'?v:v.name)).filter(Boolean).join('、');const description=H.blocks(html,'p',a=>a.itemprop==='description').map(v=>field(v.body)).sort((a,b)=>b.length-a.length)[0]||field(data.description);let cover='';try{const u=new URL(typeof data.image==='string'?data.image:data.image?.url);if(u.protocol==='https:'&&!u.username&&!u.password)cover=u.href;}catch{}return {...seed,name:H.text(data.name),description,developer:person(data.author),publisher:person(data.publisher),isbn:require('./metadata-enrichment').isbn(String(data.isbn||'')),releaseDate:H.text(data.datePublished),pages:require('./metadata-enrichment').count(String(data.numberOfPages||'')),genres:[data.genre].flat().filter(v=>typeof v==='string').map(H.text),cover,detailsUnavailable:!description};}
async function search({html,emit=()=>{}},query,id){const origin=origins[id];if(!origin)throw Error('未知书籍源');const target=origin+(id==='tstrs'?'/search?q=':'/s/')+encodeURIComponent(query),page=await html(target);if(!page)return [];const rows=parseSearch(page,id),result=[];for(const row of rows){Runtime.check();emit(row);const detail=await html(row.storeUrl);let next;try{next=parseDetail(detail,row);}catch(error){Runtime.recordFailure({kind:'parse',status:200,host:new URL(origin).hostname,label:'解析失败',message:error.message});next=row;}result.push(next);emit(next);}return result;}
module.exports={search,parseSearch,parseDetail};
