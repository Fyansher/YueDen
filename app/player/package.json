{
  "name": "unified-manager-player-core",
  "version": "0.1.0",
  "private": true,
  "description": "Integrated single-session mpv backend and native player windows",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "typescript": "5.9.3"
  }
}
